# Floor Plans

This package stores source-controlled floor-plan and graph samples for the MVP.

Rules:

- SVG is the visual layer.
- Navigation graph is separate JSON/data.
- Stable `placeId` values connect the SVG, ServicePoint mapping, and navigation entry nodes.
