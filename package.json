{
  "name": "carepath-monorepo",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm --workspace @carepath/web run dev",
    "build:web": "npm --workspace @carepath/web run build"
  }
}
